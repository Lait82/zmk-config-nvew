if SHIELD_NVEW_LEFT || SHIELD_NVEW_RIGHT

config ZMK_KEYBOARD_NAME
    default "NVEW"

config ZMK_SPLIT
    default y

config ZMK_SPLIT_BLE
    default y

endif

if SHIELD_NVEW_RIGHT
# Mitad derecha = CENTRAL (host BLE + OLED)
config ZMK_SPLIT_ROLE_CENTRAL
    default y

config ZMK_DISPLAY
    default y

config SSD1306
    default y

config ZMK_DISPLAY_STATUS_SCREEN
    default y
endif

if SHIELD_NVEW_LEFT
# Mitad izquierda = PERIPHERAL
config ZMK_SPLIT_ROLE_PERIPHERAL
    default y
endif
